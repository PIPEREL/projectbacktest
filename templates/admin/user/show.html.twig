{% extends 'base.html.twig' %} {% block title %}User{% endblock %} {% block body %}
<h1>User</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Roles</th>
            <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ user.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ user.prenom }}</td>
        </tr>
    </tbody>
</table>

<a href="{{ path('user_index') }}">back to list</a>

<a href="{{ path('user_edit', {'id': user.id}) }}">edit</a> {{ include('admin/user/_delete_form.html.twig') }}


<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Pays</th>
            <th>Ville</th>
            <th>Rue</th>
            <th>Code_postal</th>
            <th>Titre</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for adress in adresses %}
        <tr>
            <td>{{ adress.id }}</td>
            <td>{{ adress.pays }}</td>
            <td>{{ adress.ville }}</td>
            <td>{{ adress.rue }}</td>
            <td>{{ adress.codePostal }}</td>
            <td>{{ adress.titre }}</td>
            <td>
                <a href="{{ path('adresses_show', {'id': adress.id}) }}">show</a>
                <a href="{{ path('adresses_edit', {'id': adress.id}) }}">edit</a>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="7">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>





{% endblock %}